{
  "name": "@solo-compass/sources-core",
  "version": "0.0.1",
  "private": true,
  "description": "Uniform SourceAdapter contract for all data-engine source adapters",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.0.0"
  }
}
