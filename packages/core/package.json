{
  "name": "@solo-compass/core",
  "version": "0.0.1",
  "private": true,
  "description": "Domain types and shared logic — Experience, Place, User, Confidence",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.0.0"
  }
}
