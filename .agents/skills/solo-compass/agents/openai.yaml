interface:
  display_name: "Solo Compass"
  short_description: "Repo-specific patterns and workflows for solo-compass"
  default_prompt: "Use the solo-compass repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true